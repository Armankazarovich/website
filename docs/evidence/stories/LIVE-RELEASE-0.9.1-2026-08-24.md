# PiloRus Stories 0.9.1 — живое доказательство выпуска

Дата: 2026-08-24

Вердикт: **PROTECTED PATCH** для исправления воспроизведения и фоновой загрузки. Весь модуль остаётся `beta`, а не 1.0.0, из-за известных ограничений доступности.

## Выпуск

- deployed source SHA: `b8594c675eca6a208b505e575819c9ebe9a983b0`;
- previous/rollback SHA: `dd57e565d254030231969314bb7c1dbab1098b5c`;
- GitHub Actions run: `32754146479` — успешно;
- `Build on CI` — успешно;
- `Release on server` и серверный health-check — успешно;
- классификатор вернул code-only; CI-шаг изменения Prisma получил `skipped`;
- Prisma-схема, data-migrate и менеджерские записи workflow не менялись.

## Целостность данных до/после

- `/api/stories`: HTTP 200;
- до и после: 6 сторис, из них 2 LIVE и 4 VIDEO;
- нормализованный SHA-256: `575b466e89c730b25d89e1b59c9ff1e7256dca698553e8941dbe1dbb49ea33c8` — совпал;
- `/api/health`, `/`, `/catalog`, `/stories`: HTTP 200;
- `/admin/stories` без сессии штатно перенаправил на `/login`; изменения в админке не выполнялись.

## Автоматический живой браузер

| Проверка | Результат |
|---|---:|
| Stories responsive на `pilo-rus.ru` | 5/5 |
| Light → heavy → light → error → 404 HEAD | 14/14 |

Recovery-проверка использовала синтетический набор только внутри изолированного Chromium. База и файлы сайта не изменялись.

## Независимый проход по реальному тяжёлому видео

Проверено реальное LIVE-видео размером 322 650 814 байт из живого набора:

- закрытая desktop-карточка: один HEAD для размера, ноль GET/Range, `<video>` не смонтирован;
- явное открытие: три ответа HTTP 206;
- воспроизведение: `readyState` 3 → 4, время 0,03 → 2,44 сек;
- пауза: время осталось 2,463601 → 2,463601 сек;
- продолжение: время выросло до 3,942811 сек;
- звук: `muted true → false`;
- CTA: реальный переход на `/contacts`;
- mobile 390×844: панель целиком внутри viewport, доступны пауза, звук и закрытие; повторный визуальный кадр снят после `readyState=4` и движения времени 0,44 → 1,50 сек;
- ошибки консоли desktop/mobile: 0.

Результат ручного сценария: **17/17**.

## Доказательства

- `docs/evidence/stories/live-0.9.1-desktop.png`;
- `docs/evidence/stories/live-0.9.1-mobile.png`;
- `docs/evidence/stories/preview-recovery-report.md` и три recovery-скриншота;
- паспорт: `docs/evidence/stories/MODULE-PASSPORT-0.9.1.md`.

## Что не заявляется готовым

- Escape/focus-trap/`aria-dialog` открытого overlay;
- отдельные touch-targets меньше 44 px;
- сортировка сторис в админке;
- транскодирование и автоматические постеры.

Поэтому защищена и выпущена именно версия исправления 0.9.1; повышение всего модуля до `ready`/1.0.0 требует отдельного релиза.
