# ARAY / ПилоРус полный UI UX perf audit — 2026-05-06

Живая ведомость приёмки. Ничего не считать закрытым, пока не отмечены все обязательные параметры:

- Desktop: `>= 1024px`
- Tablet: `640-1023px`
- Mobile: `< 640px`
- UX: понятность, сценарий, пустые состояния, ошибки, CTA
- Design: отступы, плотность, читаемость, иерархия
- Effects: hover/active/focus только по делу, без лишнего neon/noise
- Performance: стартовая загрузка, переходы, лишние запросы, тяжёлый client JS
- Functions/buttons: все явные кнопки, quick actions, переходы, формы

## Очередь работ

| № | Зона / страницы | Статус | Результат аудита | Сделано | Не хватает / дальше |
|---|---|---|---|---|---|
| 0 | Основа: ARAY widget, dock, capsule, admin navigation context | READY_LOCAL / SMOKE_PARTIAL | ARAY грузил тяжёлый client bundle и сетевые запросы до открытия; быстрые действия исчезали в режиме переписки; ARAY-разделы были в группе `main`; drawer мог подсвечивать несколько пунктов; mobile ARAY-кнопка могла светиться, но не открывать lazy-панель | Вынесены client-safe greeting/chips; тяжёлый `ArayWidget` lazy-mount по первому `aray:*`; история/profile/voices/tracker отложены до открытия; quick actions сохранены в чате; лимит 3 smart + 3 nav; ARAY переведён в группу `aray`; drawer active по самому длинному href; hover drawer ослаблен; добавлен pending-open handshake и click fallback для mobile dock | Browser smoke на 06.05: mobile/tablet-width ARAY открывается, есть 3 smart actions, page nav, ввод, stop/collapse; desktop-wide ещё проверить при отдельном viewport |
| 1 | Header / page transitions / search / account drawer / mobile nav | READY_LOCAL / SMOKE_PARTIAL | Переходы ощущались тяжёлыми из-за route-анимаций и отсутствия явного prefetch; mobile menu мог светиться active всегда; sheets были без backdrop/scroll lock; mobile nav поллил уведомления даже на desktop; search quick переходов было 8 | Убрана framer-motion route-анимация header/content; добавлен idle prefetch частых маршрутов; mobile nav: menu active только при open, backdrop + scroll lock, спокойные rows; notification polling включается только до `lg` и только в видимой вкладке; общий bottom padding увеличен под ARAY-док; search quick переходы сокращены до 6; drawer profile ведёт на профиль | Browser smoke: search panel открывается, 6 quick transitions; account drawer виден, профиль `/cabinet/profile`; mobile menu sheet открывается. Desktop-wide и унификация search configs ещё дальше |
| 2 | Dashboard `/admin` | READY_LOCAL / SMOKE_PARTIAL | Dashboard перегружен одинаковыми карточками и mount-анимациями; hover был почти на каждой card; KPI counter создавал лишнюю работу; важные действия должны быть не больше 6 | KPI без counter/mount animation; chart без animated bars; quick actions с calm hover + prefetch; быстрый доступ владелца сокращён до 6 кнопок; browser smoke без console errors на текущем viewport | Проверить desktop-wide и узкий mobile; дальше убрать лишний edge-hover из advice/important если шумит |
| 3 | Terminal / market `/admin/orders/new`, `/admin/orders/new?mode=market`, `/admin/terminals`, `/admin/terminals/training` | READY_LOCAL / SMOKE_PARTIAL | MARKET прятал checkout; quote rows выбирали товар неполно; controls сжимались до 28px; биржа открывалась через тяжёлые стартовые запросы кассы; старый поиск клиента мог оставаться при новом телефоне; рабочий пульт терминалов был ниже длинного обучения; terminal action buttons могли зависнуть без `finally` | Worker: checkout виден в MARKET, quote rows через `handleSelectProduct`, cart controls >= h-10/w-10; промо-chip уже не fake button; URL `mode=market` принудительно открывает биржу; fake demand заменён на `нет данных`; подбор варианта на tablet складывается вертикально; кассовые смены грузятся только для кассы/центра смен; delivery rates грузятся только при калькуляторе; client lookup очищается и отменяется; оформление требует клиента и адрес честно; `/admin/terminals` получил 3 быстрых входа, пульт поднят выше мастера; ops/integration actions защищены try/finally | Browser smoke: market load без новых console warn/error, fake demand отсутствует, товар → корзина → оформление работает; `/admin/terminals` и training открываются без новых console warn/error. Desktop-wide ещё проверить отдельным viewport |
| 4 | Orders: `/admin/orders`, detail, trash | READY_LOCAL / SMOKE_PARTIAL | Список грузил все заказы без лимита и автообновлялся каждые 30 секунд даже в фоне; `COMPLETED` ошибочно мог попадать в самовывоз; mobile cards не давали нормального выбора; quick view не отменял fetch при смене заказа; detail/trash имели тесные mobile actions, слабые empty/error states и busy-кнопки без жёсткого сброса | `/admin/orders`: серверная порционная загрузка 160/320/..., `Показать ещё`, автоrefresh только в видимой вкладке и раз в 60с, mobile checkbox + tel link, без active-scale; kind logic исправлен. Quick view aborts stale fetch. Detail/trash: приняты правки агента по 44px touch targets, try/catch/finally, toasts, блокировке закрытия во время destructive action, mobile trash cards, empty states; detail `COMPLETED` не считается самовывозом, unknown terminal fields честно `Не указано` | Browser smoke: list/filters/CSV без новых warn/error; quick view открывается; full detail открывается; trash открывается. Desktop-wide отдельным viewport позже |
| 5 | Clients / CRM / tasks / workflows | READY_LOCAL / SMOKE_PARTIAL | Clients тянули всю историю заказов в карточки и не имели локального поиска; save/delete/reset могли молча провалиться; CRM/tasks/workflows местами принимали `{error}` как успешный ответ; lead create мог закрыть форму до успеха; tasks скрывали отсутствие заказа вместо честного relation state; workflows/automation показывали fake success без `res.ok` | Clients: агрегаты считаются отдельно, в UI идут последние 5 заказов + полная статистика, добавлен локальный поиск по имени/email/телефону/адресу, toast errors для save/delete/reset. CRM/tasks/workflows: приняты правки агента — проверка payload/res.ok, rollback failed move, manual refresh busy, no fake sync/seed success, create lead closes only after success, task relation `Без заказа` | Browser smoke: `/admin/clients`, search empty state, `/admin/crm`, `/admin/tasks`, `/admin/workflows`, `/admin/crm/automation` без новых console warn/error. Desktop-wide ещё отдельно |
| 6 | Products / categories / inventory / import / media / watermark | READY_LOCAL / SMOKE_PARTIAL | Categories/products/watermark/media могли показывать success без подтверждения API; склад на tablet оставался тесной таблицей; disabled beta-кнопки шумели в строках; media рендерила слишком много карточек; inventory server передавал Decimal в client component и давал Next warning | Приняты правки агента: res.ok/data.ok checks, try/catch/finally, optimistic rollback, честные error states, media pagination/render batches, inventory tablet до 1023px -> cards, beta movement/import removed from rows/header. Локально добавлена сериализация Decimal в `app/admin/inventory/page.tsx` | Browser smoke: products/categories/inventory/import/media/watermark открываются без новых warn/error; inventory Decimal warning исчез. Desktop-wide отдельным viewport позже |
| 7 | Delivery / rates | READY_LOCAL / SMOKE_PARTIAL | Архив доставок грузился вместе с активными заказами и `items`; сумма доставки считала самовывоз; status select и rates формы могли молча провалиться; API тарифов мог отдавать 500 на битом JSON/NaN/удалённом id; mobile editing требовал безопасных touch states | Активные/самовывоз/архив разделены, архив `select + take 50`, доставка считает только активные доставки; status select проверяет `res.ok` и показывает ошибку; rates load/save/create/delete с общим `loadRates`, busy/error states и фиксированным target id; API тарифов валидирует JSON/числа, даёт 404 для отсутствующего тарифа, write роли включают `MANAGER`; `tsc` и browser smoke `/admin/delivery`, `/admin/delivery/rates` без новых warn/error | Desktop/mobile visual pass ещё будет в финальном сквозном smoke; пока новых runtime ошибок нет |
| 8 | Analytics / finance / promotion / promotions / reviews / email / notifications | READY_LOCAL / SMOKE_PARTIAL | Finance `to` терял последний день; chart был hover/title-only; analytics считал top products/выручку по отменённым или удалённым заказам; email import показывал ложный успех; reviews/posts/services/promotions/notifications имели тесные mobile actions и слабую проверку API | Finance API парсит `to` как конец дня и валидирует диапазон; график кликабельный/focusable с видимой датой/суммой; analytics считает live orders без `CANCELLED` и `deletedAt`; email import пишет в `NewsletterSubscriber` с дедупом и ошибками; reviews/posts/services/promotions/notifications проверяют `res.ok`, показывают ошибки, получили safer mobile actions; promotions `validUntil` сохраняется концом дня | `tsc` чистый; browser smoke `/admin/analytics`, `/admin/finance`, `/admin/promotion`, `/admin/promotions`, `/admin/reviews`, `/admin/email`, `/admin/notifications`, `/admin/posts`, `/admin/services` без новых console warn/error |
| 9 | Settings / site / appearance / business / staff / health / help / aray-lab | READY_LOCAL / SMOKE_PARTIAL | Найдены реальные API honesty и UX drift: SMTP quick test считал успешный `{ok:true}` ошибкой; `/admin/site` показывал `Сохранено` без `res.ok`; health UI держал зашитые 10/15 проверок и терял ARAY-категорию; help отправлял старое событие `aray:fill`; staff role matrix расходилась с `lib/permissions` | Исправлены SMTP/Telegram quick test statuses, site save теперь фильтрует только свои keys и показывает ошибку только по ответу API, health count/category берутся с API, SMTP health реально проверяет конфиг/verify, help использует `aray:prompt`, staff matrix берёт доступы из `getAccessibleSections`, touch targets staff-actions увеличены | `tsc` чистый; browser smoke `/admin/settings`, `/admin/site`, `/admin/appearance`, `/admin/business/settings`, `/admin/staff`, `/admin/health`, `/admin/help`, `/admin/aray-lab` без новых console warn/error |
| 10 | Store / cabinet global effects where ARAY mounted | READY_LOCAL / SMOKE_FULL_LOCAL | ARAY lazy improves start; push prompts duplicated по аудиту; store/cabinet shell дублирует тяжёлые запросы по categories/types/sizes; logout мог оставлять защищённый UI до refresh; mobile/tablet account/search/cart states были тихими; helper размеров имел риск кодировки для `×`; cabinet layout скрывал `children` за client-only transition и отдавал пустой `main`; checkout делал redirect во время render/hook-order warning; track показывал hydration mismatch по текущему времени; profile/appearance содержали битую кириллицу и пустые loading states | Вынесен общий cached `getStoreShellData()` на 60с; logout refresh/redirect исправлен; PWA install больше не просит push, global push prompt скрыт на явных notification pages; mobile bottom nav/tablet rail подсвечивают search/cart/account/wishlist; drawer active нормализует trailing slash; helper размеров переписан через Unicode escape; cabinet layout рендерит children сразу без client-only transition; checkout redirect перенесён в стабильный effect после mount; track `lastRefreshed` больше не создаёт SSR/client mismatch; profile/appearance переведены в нормальный UTF-8 и больше не возвращают пустой `null` | `tsc` чистый; browser sweep store/cabinet: `/`, `/catalog`, `/catalog/listvennitsa`, `/product/brus-antisept-sosna`, `/checkout`, `/calculator`, `/delivery`, `/promotions`, `/services`, `/news`, `/about`, `/contacts`, `/track`, `/track/[id]`, `/privacy`, `/terms`, `/wishlist`, `/cabinet`, `/cabinet/orders`, `/cabinet/profile`, `/cabinet/reviews`, `/cabinet/media`, `/cabinet/subscriptions`, `/cabinet/history`, `/cabinet/notifications`, `/cabinet/appearance` без runtime overlay; финальный cabinet smoke без console warn/error |

## Master route order принят 06.05.2026

1. Foundation: ARAY widget/dock/capsule, navigation context, quick actions, search, account drawer, mobile nav.
2. Header/page transitions/search/account drawer/mobile nav.
3. Рабочий стол: `/admin`.
4. Терминал/биржа: `/admin/orders/new`, `/admin/orders/new?mode=market`, `/admin/terminals`, `/admin/terminals/training`.
5. Заказы: `/admin/orders`, `/admin/orders/[id]`, `/admin/orders/trash`.
6. Клиенты/CRM/задачи: `/admin/clients`, `/admin/crm`, `/admin/crm/automation`, `/admin/tasks`, `/admin/workflows`.
7. Управление бизнесом: `/admin/products`, `/admin/products/[id]`, `/admin/products/new`, `/admin/products/audit`, `/admin/products/import-prices`, `/admin/categories`, `/admin/inventory`, `/admin/import`, `/admin/media`, `/admin/images/fix`, `/admin/watermark`, `/admin/watermark/recovery`.
8. Доставка/операции: `/admin/delivery`, `/admin/delivery/rates`.
9. Маркетинг/контент/финансы: `/admin/analytics`, `/admin/finance`, `/admin/promotion`, `/admin/promotions`, `/admin/reviews`, `/admin/email`, `/admin/notifications`, `/admin/posts`, `/admin/services`.
10. Система/обучение: `/admin/settings`, `/admin/site`, `/admin/appearance`, `/admin/business/settings`, `/admin/staff`, `/admin/health`, `/admin/help`, `/admin/aray-lab`.
11. Кабинет: `/cabinet`, `/cabinet/orders`, `/cabinet/orders/[id]`, `/cabinet/profile`, `/cabinet/reviews`, `/cabinet/media`, `/cabinet/subscriptions`, `/cabinet/history`, `/cabinet/notifications`, `/cabinet/appearance`, `/wishlist`.
12. Store: `/`, `/catalog`, `/catalog/[category]`, `/product/[slug]`, `/cart`, `/checkout`, `/calculator`, `/delivery`, `/promotions`, `/services`, `/news`, `/news/[slug]`, `/about`, `/contacts`, `/track`, `/track/[orderId]`, `/privacy`, `/terms`, `/wishlist`.

## Agent audit intake 06.05.2026

- Lorentz: master route map, sources, P0 requirements, nav/permissions conflicts accepted.
- Bacon: products/categories/inventory/import/media/watermark accepted. P0: categories/products/watermark can show false success without API checks.
- Banach: orders/terminal/market/clients/CRM/tasks/delivery accepted. P1: heavy order page, whole-list loads, stuck busy states, wrong fulfillment logic.
- Socrates: analytics/finance/marketing/settings/store/cabinet accepted. P1/P2: finance date range, email import not saved, role policy drift, shell duplicated queries, false success in settings/site.

## Принятые агентские находки

- Navigation/Menu: broken `AdminMenuPopup`, ARAY group in `main`, drawer multi-active, noisy hover.
- Terminal/Market: hidden checkout in MARKET, quote row partial select, fake promo button, small touch targets.
- Forms/Tables: mobile terminal risk, small actions, table min-width/card layout, weak empty states.
- Performance: ARAY client imported server agent, ARAY loaded network before open, mobile nav polling, duplicated search/menu configs, heavy order page.
- Design/UX: modal z-index mismatch, mobile sheets without backdrop, global `.fixed/.h-screen` overrides risk.

## Текущий порядок

1. Закрыть ARAY/капсулу до smoke-check.
2. Закрыть header/navigation transitions.
3. Закрыть dashboard `/admin`.
4. Закрыть terminal/market.
5. Идти по группам навигации сверху вниз: orders → clients/CRM/tasks → products/inventory → delivery → marketing/finance → settings/help.

## Intake / local fixes - 2026-05-06 late pass

Status: `READY_LOCAL / QUALITY_PASSED`

- [x] ARAY capsule: removed the `Все разделы` dropdown; capsule now shows only contextual smart page links and the current group/page label.
- [x] ARAY perf: admin assistant no longer mounts heavy chat/widget immediately on every admin route; it mounts on explicit ARAY open or delayed warm idle.
- [x] Admin smart mobile nav: mobile menu no longer renders all admin sections as one long sheet; it shows group tabs, current/selected group, up to 6 page rows, and routes overflow to search.
- [x] Admin header mobile: left title can shrink/truncate, appearance/account/extra controls no longer force 390px overflow; core header touch targets are 44px.
- [x] CRM: removed duplicate in-page `ARAY CRM` heading, kept shell title as source of truth, added skeleton loading board, deferred lead API until lead tab, raised CRM controls to 44px.
- [x] Store/cabinet shell: tablet bottom nav duplication removed; mobile footer and tablet footer now reserve space for fixed nav/rail.
- [x] Store UX: `/wishlist` no longer highlights account in mobile bottom nav; catalog filter sheet z-index is above bottom nav; account drawer chunk warms after idle.
- [x] Orders/inventory touch pass: order status chips, mobile checkbox area, phone links and quick-view prev/next are 44px; inventory toast avoids mobile bottom nav.
- [x] Automation: removed full `router.refresh()` every 30s from `/admin/crm/automation`; client-owned API refresh remains responsible for its own data.
- [x] Verification: `npx tsc --noEmit --pretty false --incremental false` passed; `npm run quality` passed; in-app browser route pass showed no fresh console errors and no `Все разделы` on checked routes.
- [ ] Still next: real mobile viewport screenshot pass for `/admin/crm/automation`, `/admin/appearance`, `/admin/orders/new`, `/admin/products`, `/admin/site`; deeper API pagination for `/catalog`, `/admin/inventory`, `/admin/products`, `/admin/clients`.
